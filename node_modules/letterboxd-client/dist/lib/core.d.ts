import 'isomorphic-fetch';
export declare const BASE_URL = "https://api.letterboxd.com/api/v0";
export interface Auth {
    apiKey?: string;
    apiSecret?: string;
    accessToken?: string;
}
export interface APIResponse {
    status: number;
    data?: any;
}
export declare function request<T extends APIResponse>(opts: {
    method: 'get' | 'post' | 'patch' | 'delete';
    path: string;
    auth?: Auth;
    params?: Record<string, any>;
    body?: Record<string, any>;
    headers?: Record<string, string>;
}): Promise<{
    res: Response;
} & T>;
